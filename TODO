- lib.sh, build.sh: add support for additional parameter, to build only a specific package
- add lynx text browser ( or https://www.brow.sh )
- add mplayer support (depends on availability of audio library on bb10 )
